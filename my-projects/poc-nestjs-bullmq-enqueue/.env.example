PORT=3000
REDIS_HOST=localhost
REDIS_PORT=6379

# Credenciais do dashboard Bull Board (/queues).
# Se ambas ficarem vazias, a rota fica liberada (apenas para dev local).
# Defina antes de expor a app fora do localhost.
QUEUES_USER=
QUEUES_PASS=
